# Route shylockhg.me to dnsmasq (listening on the Tailscale IP)
# so the local machine uses dnsmasq for these domains too,
# instead of forwarding everything to Headscale DNS (100.100.100.100).
[Resolve]
DNS=100.64.0.2
Domains=~shylockhg.me
